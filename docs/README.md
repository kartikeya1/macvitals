# docs/

Planning docs and repo assets.

- **WEB_APP_PLAN.md** — product & technical plan for the hosted web version, with the phase roadmap.
- **social-preview.png** — 1200×630 OpenGraph/social card (also used at the top of the main README). You can upload this under GitHub → Settings → Social preview.
- **flow.svg** — the "get → run → view" flow diagram embedded in the README.

_Nice-to-have:_ real in-app UI screenshots (landing + dashboard) can be dropped here and linked from the README — capture them from a local `npm run dev` and redact any serial/UUIDs first.
